import { infraSelfServicePortalPlugin } from './plugin';

describe('infra-self-service-portal', () => {
  it('should export plugin', () => {
    expect(infraSelfServicePortalPlugin).toBeDefined();
  });
});
